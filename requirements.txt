# 抖音热点深度分析工具 — 依赖清单
# Python 3.10+

# 大模型调用（OpenAI 兼容协议，可对接 DeepSeek / 通义 / Kimi 等）
openai>=1.30.0

# 数据校验与序列化
pydantic>=2.6.0

# CLI
typer>=0.12.0
rich>=13.7.0

# 可选：Web API
fastapi>=0.110.0
uvicorn>=0.29.0

# 配置
python-dotenv>=1.0.0

# 开发/测试
pytest>=8.0.0
httpx>=0.27.0
